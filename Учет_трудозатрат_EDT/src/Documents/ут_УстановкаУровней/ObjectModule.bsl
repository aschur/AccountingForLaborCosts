
Процедура ОбработкаПроведения(Отказ, РежимПроведения)

	Движения.ут_УровниРаботника.Записывать = Истина;
	Для Каждого ТекСтрокаУровни Из Уровни Цикл
		Движение = Движения.ут_УровниРаботника.Добавить();
		Движение.Период = Дата;
		Движение.Уровень = ТекСтрокаУровни.Уровень;
		Движение.Сумма = ТекСтрокаУровни.Сумма;
	КонецЦикла;

КонецПроцедуры
